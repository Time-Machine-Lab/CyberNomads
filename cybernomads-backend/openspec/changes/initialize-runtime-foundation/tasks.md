## 1. Startup Bootstrap Flow

- [x] 1.1 在应用启动链路中增加 runtime bootstrap 编排入口，并定义启动前初始化与 ready 状态之间的执行顺序
- [x] 1.2 实现当前工作目录下 `cybernomads/` 运行根目录的检测逻辑，确保根目录缺失时创建、已存在时仅跳过根目录创建
- [x] 1.3 实现固定子目录 `product/`、`strategy/`、`work/` 的补齐逻辑，确保缺失目录会被创建
- [x] 1.4 定义并接入 bootstrap 错误类型与启动失败返回路径，保证初始化失败时系统不会静默进入 ready 状态

## 2. SQLite And Runtime SQL Assets

- [x] 2.1 定义 SQLite 运行时数据库文件的位置与命名规则，并实现创建或打开数据库文件的适配逻辑
- [x] 2.2 增加运行时 SQL 资产目录与首个 bootstrap SQL 脚本，只创建系统级初始化表，不创建正式业务表
- [x] 2.3 实现运行时 SQL 脚本加载机制，确保 SQL 来源为后端自带运行时资产而不是 `docs/sql/`
- [x] 2.4 实现 SQL 脚本执行记录机制，保证重复启动时不会重复执行已完成脚本

## 3. Verification And Guardrails

- [x] 3.1 增加覆盖首次启动场景的测试，验证根目录、固定子目录、SQLite 文件和系统初始化表会被正确准备
- [x] 3.2 增加覆盖重复启动场景的测试，验证根目录已存在时仍会继续检查 SQLite 与 SQL 脚本执行
- [x] 3.3 增加失败场景测试，验证 SQLite 创建失败或 SQL 执行失败时启动会明确报错
- [x] 3.4 更新启动相关说明与运行时目录说明，确保实现与现有架构文档、开发规范文档保持一致
