## 1. 前端账号数据层与联调基础

- [x] 1.1 在 `cybernomads-ui/src/entities/account/` 中重建与后端契约对齐的 DTO、展示模型和状态映射逻辑，替换旧的 mock `AccountRecord` 结构。
- [x] 1.2 将账号模块 API 封装改造为真实 HTTP 请求，覆盖列表、详情、更新、删除、恢复、授权尝试、授权验证和可用性检查，同时保持非账号模块继续使用 mock。
- [x] 1.3 在 `cybernomads-ui` 开发环境中补齐账号模块本地联调所需的 `/api` 转发方案，并明确账号模块真实后端接入方式。

## 2. 账号列表页改造

- [x] 2.1 改造 `cybernomads-ui/src/pages/accounts/list/`，使列表页基于后端 `AccountSummary` 渲染账号行信息和状态展示。
- [x] 2.2 将平台统计卡改造为基于真实账号列表结果的前端聚合结果，不再依赖硬编码统计数据。
- [x] 2.3 为列表页补齐加载态、空态、错误态以及删除/恢复后的刷新行为，并整理行级按钮的真实交互语义。

## 3. 账号详情页改造

- [x] 3.1 改造 `cybernomads-ui/src/pages/accounts/detail/`，使用后端 `AccountDetail` 渲染详情页，并将平台与平台账号 UID 调整为只读身份信息。
- [x] 3.2 按后端允许的字段边界实现详情页基础资料编辑与保存，只保留显示名、备注、标签和平台扩展字段的可编辑能力。
- [x] 3.3 接通详情页的逻辑删除、恢复和状态刷新流程，确保页面可以反映后端确认后的生命周期状态。

## 4. 授权与可用性流程对接

- [x] 4.1 将当前“保存凭证配置”改造为真实授权尝试与授权验证流程，至少支持首版 token 或 cookie 类直接凭证输入。
- [x] 4.2 将当前“验证连接”动作改造为真实可用性检查流程，并把后端返回的授权/可用性结果映射到详情页状态区域。
- [x] 4.3 对二维码区、凭证原文回显、凭证历史记录和终端日志区做一期降级处理，避免页面依赖未发布的后端契约。

## 5. 验证与收尾

- [x] 5.1 为账号模块前端数据映射和核心页面行为补充测试或最小可回归验证，覆盖真实后端 DTO 到 UI 展示模型的关键转换。
- [x] 5.2 进行本地联调冒烟验证，确认账号列表页和详情页在“账号模块真实后端 + 其他模块仍为 mock”的条件下可正常访问和操作。
