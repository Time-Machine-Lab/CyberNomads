## 1. Contract Readiness

- [x] 1.1 读取并校验 `docs/sql/` 中的产品模块 SQL 文档，确认其已覆盖当前实现所需的最小数据结构
- [x] 1.2 读取并校验 `docs/api/` 中的产品模块 API 文档，确认其已覆盖创建、更新、列表、详情接口
- [x] 1.3 如果实现需求与现有 `docs/sql/` 或 `docs/api/` 不一致，先更新对应顶层文档，再进入代码开发

## 2. Product Module Implementation

- [x] 2.1 在 `src/modules/products/` 中建立产品模块的领域对象、类型定义和应用服务骨架
- [x] 2.2 实现产品创建能力，支持产品名称与完整正文的写入
- [x] 2.3 实现产品更新能力，保证同一产品标识下返回最新语义
- [x] 2.4 实现产品列表能力，返回摘要信息而不是完整正文
- [x] 2.5 实现产品详情能力，返回完整 Markdown 正文并承担完整上下文提供能力

## 3. Integration And Verification

- [x] 3.1 将产品模块对接到 SQLite 存储和正文内容持久化能力
- [x] 3.2 增加产品模块 HTTP Controller，并对齐 `docs/api/` 契约
- [x] 3.3 增加产品模块测试，覆盖创建、更新、列表、详情场景
- [x] 3.4 验证当前阶段未实现产品删除、状态机和版本管理能力
