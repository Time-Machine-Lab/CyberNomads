## 1. 顶层契约文档

- [x] 1.1 创建 `docs/sql/accounts.sql`，定义账号模块所需的最小结构化存储边界，包括稳定账号对象、待验证授权尝试和引用语义。
- [x] 1.2 创建 `docs/api/accounts.yaml`，定义账号模块的顶层接口边界，包括账号创建、列表、详情、更新、授权尝试、授权验证、可用性检查、逻辑删除和恢复。

## 2. 规格与契约对齐

- [x] 2.1 将 `account-contracts` 规格逐条映射到 `docs/sql/accounts.sql` 与 `docs/api/accounts.yaml`，确保稳定身份、详情脱敏、当前凭证与授权尝试分离、逻辑删除与恢复等约束被顶层契约覆盖。
- [x] 2.2 将 `account-module-runtime` 规格逐条映射到顶层契约，确保最低消费条件、受控凭证解析前置条件和授权尝试切换规则能够被后续实现直接承接。

## 3. 交付校验

- [x] 3.1 校验账号模块文档命名与边界符合项目规则：一张表对应一个 `.sql` 文件，一组接口对应一个 `.yaml` 文件。
- [x] 3.2 完成文档自检，确认账号模块后续实现可以以 `docs/api/accounts.yaml` 和 `docs/sql/accounts.sql` 作为单一真理继续推进。
