## 1. Prompt Construction

- [x] 1.1 重构任务拆分 prompt 构建逻辑，按“引流工作信息 / 产品信息 / 策略信息 / 任务拆分Skill信息 / 基础路径信息 / 规则”输出结构化消息
- [x] 1.2 在任务拆分请求中显式注入 Cybernomads 目录绝对路径，以及相对化的引流工作目录与任务拆分 `SKILL.md` 路径
- [x] 1.3 简化规则段，仅保留“解析相对路径、访问 Skill、读取 Skill、再开始任务拆分”这类入口规则
- [x] 1.4 移除业务 prompt 中重复描述的任务拆分细节，确保拆分规则只保留在 Skill 内

## 2. Runtime Resolution And Validation

- [x] 2.1 接入 Cybernomads 根目录解析，以及基于根目录生成相对化引流工作目录和任务拆分 Skill 文件路径
- [x] 2.2 在调用 Agent 服务前校验 Cybernomads 根目录、引流工作目录和任务拆分 Skill 文件路径均可用
- [x] 2.3 统一失败语义，确保缺少必要运行时输入时直接进入准备失败而不是发送残缺请求

## 3. Verification

- [x] 3.1 补充单元测试，验证任务拆分 prompt 已加载引流工作 ID、产品 ID、策略 ID、产品正文、策略正文、Cybernomads 根目录绝对路径、相对工作目录和相对 Skill 文件路径
- [x] 3.2 补充失败场景测试，覆盖根目录缺失、Skill 路径缺失或工作目录无效时不会调用 Agent 服务
- [x] 3.3 补充集成验证，证明创建或更新引流工作时发送给 Agent 的任务拆分请求符合新的结构化提示词约束
