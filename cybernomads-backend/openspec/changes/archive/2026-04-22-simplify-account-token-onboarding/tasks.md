## 1. 顶层契约文档

- [x] 1.1 更新 `docs/api/accounts.yaml`，为已有账号换令牌流程补充 challenge-aware 的授权尝试启动与验证语义。
- [x] 1.2 新增 `docs/api/account-onboarding-sessions.yaml`，定义账号接入会话 Controller 的启动、解析/轮询、结果读取和最终完成接入接口。
- [x] 1.3 新增 `docs/sql/account_onboarding_sessions.sql`，定义接入会话表及其待解析输入引用、候选令牌引用、解析结果和消费状态语义。

## 2. 后端接入会话模块

- [x] 2.1 在 `cybernomads-backend` 中新增账号接入会话的类型、仓储和持久化实现，并接入现有 sqlite 运行时。
- [x] 2.2 实现账号接入会话 service，覆盖会话启动、challenge 返回、令牌解析结果写入、最终新建/恢复/返回既有账号引用等主流程。
- [x] 2.3 实现并挂载账号接入会话 Controller，使新增账号流程具备独立 HTTP 入口。

## 3. 账号模块与平台能力改造

- [x] 3.1 扩展 `src/ports/account-platform-port.ts` 及对应 stub adapter，使平台能力同时支持新增账号接入会话和已有账号换令牌两条路径。
- [x] 3.2 更新 `src/modules/accounts/` 的 types、service 和 controller，使已有账号授权尝试启动结果能够返回 challenge 摘要并保持待验证隔离语义。
- [x] 3.3 补齐接入会话 finalize 与账号模块之间的新建、恢复、既有账号冲突处理和会话消费清理逻辑。

## 4. 前端账号页面对接

- [x] 4.1 改造账号列表页和路由，增加真实“新增账号”入口并接入独立账号接入页。
- [x] 4.2 实现新增账号接入页，完成平台选择、challenge 展示、令牌解析结果确认和最终新建/恢复跳转。
- [x] 4.3 重构账号详情页，把手工录入与扫码授权合并到统一“令牌接入”区域，并接通已有账号更换当前令牌的后端接口。

## 5. 验证与回归

- [x] 5.1 为账号接入会话、已有账号换令牌和平台能力规范化返回补充后端测试。
- [x] 5.2 为列表页新增入口、接入页流程和详情页统一令牌接入区补充前端测试或最小可回归验证。
- [x] 5.3 进行本地联调冒烟，确认新增账号、恢复账号、已有账号换令牌和连接验证流程都能贯通。
