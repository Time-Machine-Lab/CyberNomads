## 1. Visual Foundation

- [x] 1.1 Extract shared high-fidelity design tokens for colors, glow states, panel surfaces, typography, spacing, and ghost borders under the shared style layer
- [x] 1.2 Build reusable high-fidelity primitives for shell panels, dense cards, status badges, section headers, and placeholder asset containers
- [x] 1.3 Prepare temporary branded assets and placeholder resources for missing icons, avatars, and screenshot-derived visual substitutes

## 2. Unified Product Shell

- [x] 2.1 Rebuild the global desktop shell with CyberNomads branding, Chinese primary navigation labels, and the `_4`-inspired left sidebar structure
- [x] 2.2 Implement sidebar collapse behavior, active module states, and child-page context cues without breaking the approved five-module information architecture
- [x] 2.3 Align the top context region and desktop layout rules so common desktop widths preserve the intended asymmetry and panel balance

## 3. Key High-Fidelity Reference Pages

- [x] 3.1 Rebuild the workspace list page to match the approved card density, status treatment, and action hierarchy from the reference design
- [x] 3.2 Rebuild the workspace creation flow to match the step structure, selection cards, and primary action treatment from the reference design
- [x] 3.3 Rebuild the workspace execution console with the approved task graph viewer, logs panel, intervention surface, and top execution navigation
- [x] 3.4 Rebuild the Agents empty state and OpenClaw configuration page to match the approved empty-state messaging, configuration layout, and diagnostic panel composition

## 4. High-Fidelity Workflow Support

- [x] 4.1 Upgrade shared mock models so high-density cards, execution nodes, timestamps, statuses, and config panels read from common domain data
- [x] 4.2 Upgrade mock scenarios to cover visually complete empty, populated, editing, running, and failure states for the rebuilt pages
- [x] 4.3 Rework Assets, Strategies, and Accounts pages so their list, editor, and detail flows use the new shell, visual primitives, and module-specific page compositions
- [x] 4.4 Preserve sensible workflow interactions for save, deploy, initialize, test connection, tab switching, node inspection, and intervention submission inside the high-fidelity layouts

## 5. Verification And Fidelity Review

- [x] 5.1 Verify the rebuilt pages against the reference screenshots and document any intentional deviations caused by missing source assets
- [x] 5.2 Validate common desktop viewport behavior, collapsed sidebar usability, and module-to-child-page context continuity
- [x] 5.3 Update affected frontend documentation or notes if implementation decisions refine placeholder asset handling or page-specific interaction boundaries
